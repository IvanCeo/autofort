APP_ENV=DEV

APP_PORT=8080

# Postgres (в docker-сети)
PGHOST=db
PGPORT=5432
PGUSER=app
PGPASSWORD=password
PGDATABASE=autofort
PGSSLMODE=disable

MIGRATIONS_DIR=migrations

# Worker-order (в docker-сети)
WORK_ORDER_HOST_DEV=worker-order
WORK_ORDER_PORT_DEV=8001
WORK_ORDER_HOST_PROD=worker-order
WORK_ORDER_PORT_PROD=8001

ADMIN_USER=dmitry
ADMIN_PASS=Rjvcjvjktw1

# Postgres data
POSTGRES_DB=autofort
POSTGRES_USER=app
POSTGRES_PASSWORD=password
